# Types Directory

This directory contains system-wide shared TypeScript interfaces and type aliases:

- `index.ts` - Interface contracts for primary domain models including `Citizen`, `Complaint`, `PaymentRecord`, `FleetVehicle`, and `Stop`.
