# Constants Directory

This directory contains system-wide configuration constants:

- `theme.ts` - Centralized design token repository for spacing, color schemes, elevations, z-indices, and animation transitions to ensure consistency across pure CSS, Tailwind, and JS layout configurations.
