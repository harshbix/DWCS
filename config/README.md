# Config Directory

This directory contains run-time environment and app config wrappers for the Next.js frontend:

- `env.ts` - Client environment loader that validates parameters against `schemas/env.schema.ts` on startup.
