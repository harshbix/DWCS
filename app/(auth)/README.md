# Authentication Route Group

This directory contains route segments and page layouts dedicated to system security and user login/signup operations:

- `login/page.tsx` - Base portal login page (placeholder).
