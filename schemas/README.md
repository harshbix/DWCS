# Schemas Directory

This directory contains system-wide schemas, primarily using **Zod** for run-time validation:

- `env.schema.ts` - Zod schema definitions for frontend and backend environment variables.
