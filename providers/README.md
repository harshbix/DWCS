# Providers Directory

This directory contains React Context wrappers providing global state configurations to the client tree:

- `query-provider.tsx` - Instantiates the TanStack Query `QueryClient` container with standard caching thresholds and query refresh policies.
