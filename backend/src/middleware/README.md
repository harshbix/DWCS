# Backend Middleware Folder

This folder contains Express routing filters:

- `errorHandler.ts` - Catches express errors and maps them to standard API responses.
- `requestValidator.ts` - Schema-validator middleware using Zod schemas.
