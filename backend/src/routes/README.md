# Backend Routes Folder

This folder handles route definitions and maps paths to controllers:

- `index.ts` - Root Router mounting system routes and reserving Phase 2 endpoint paths.
- `health.ts` - Diagnostic responder indicating system state.
